# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  accessToken: String!
  refreshToken: String!
  user: User!
}

input CloseTicketInput {
  ticketId: Int!
}

input CreateFloorInput {
  floorNumber: Int!
  parkingLotId: Int!
}

input CreateParkingLotInput {
  address: String!
  city: String!
  code: String!
  country: String!
  latitude: Float!
  longitude: Float!
  name: String!
  postalCode: String!
  state: String!
}

input CreatePriceInput {
  hourlyRate: Float!
  vehicleType: VehicleType!
}

input CreateSlotInput {
  floorId: Int!
  slotNumber: String!
}

input CreateTicketInput {
  slotId: Int!
  vehicleId: Int!
}

input CreateUserInput {
  email: String!
  fullName: String!
  password: String!
  role: UserRole
}

input CreateVehicleInput {
  color: String!
  manufacturer: String!
  model: String!
  ownerId: Float
  registrationNumber: String!
  type: VehicleType!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  closeTicket(input: CloseTicketInput!): ParkingTicket!
  createParkingFloor(input: CreateFloorInput!): ParkingFloor!
  createParkingLot(input: CreateParkingLotInput!): ParkingLot!
  createParkingSlot(input: CreateSlotInput!): ParkingSlot!
  createPricing(input: CreatePriceInput!): ParkingPrice!
  createTicket(input: CreateTicketInput!): ParkingTicket!
  createUser(input: CreateUserInput!): User!
  createVehicle(input: CreateVehicleInput!): Vehicle!
  deleteParkingLot(id: Int!): Boolean!
  deleteVehicle(id: Int!): Boolean!
  generateParkingSlots(count: Int!, floorId: Int!): [ParkingSlot!]!
  login(input: LoginInput!): AuthResponse!
  makePayment(amount: Float!, ticketId: Int!): Payment!
  occupySlot(slotId: Int!): ParkingSlot!
  releaseSlot(slotId: Int!): ParkingSlot!
  signup(input: SignUpInput!): User!
  updateParkingLot(input: UpdateParkingLotInput!): ParkingLot!
  updateProfile(input: UpdateProfileInput!): User!
  updateVehicle(id: Int!, input: UpdateVehicleInput!): Vehicle!
}

input ParkingFilterInput {
  address: String
  name: String
}

type ParkingFloor {
  createdAt: DateTime!
  displayOrder: Int!
  floorNumber: Int!
  id: ID!
  name: String!
  updatedAt: DateTime!
}

type ParkingLot {
  address: String!
  city: String!
  code: String!
  country: String!
  createdAt: DateTime!
  id: ID!
  latitude: Float!
  longitude: Float!
  name: String!
  postalCode: String!
  state: String!
  status: ParkingLotStatus!
  updatedAt: DateTime!
}

enum ParkingLotStatus {
  ACTIVE
  INACTIVE
  MAINTENANCE
}

type ParkingPrice {
  active: Boolean!
  hourlyRate: Float!
  id: ID!
  vehicleType: VehicleType!
}

type ParkingSlot {
  createdAt: DateTime!
  hasChargingStation: Boolean!
  id: ID!
  isEnabled: Boolean!
  maximumHeight: Float!
  slotNumber: String!
  status: ParkingSlotStatus!
  type: ParkingSlotType!
  updatedAt: DateTime!
}

enum ParkingSlotStatus {
  AVAILABLE
  MAINTENANCE
  OCCUPIED
  OUT_OF_SERVICE
  RESERVED
}

enum ParkingSlotType {
  BIKE
  CAR
  ELECTRIC
  HANDICAPPED
}

type ParkingTicket {
  amount: Float!
  createdAt: DateTime!
  duration: Float!
  entryTime: DateTime!
  exitTime: DateTime
  id: ID!
  status: TicketStatus!
  ticketNumber: String!
  totalAmount: Float!
  updatedAt: DateTime!
}

type Payment {
  amount: Float!
  id: ID!
  status: PaymentStatus!
  ticketId: Float!
}

enum PaymentStatus {
  FAILED
  PENDING
  SUCCESS
}

type Query {
  activeTickets: [ParkingTicket!]!
  availableSlots: [ParkingSlot!]!

  """Returns application health status."""
  health: String!
  me: User!
  parkingDashboard(id: Int!): String!
  parkingLot(id: Int!): ParkingLot!
  parkingLots: [ParkingLot!]!
  parkingSearch(filter: ParkingFilterInput): [ParkingLot!]!
  payment(id: Int!): Payment
  payments: [Payment!]!
  ticket(id: Int!): ParkingTicket
  tickets: [ParkingTicket!]!
  users: [User!]!
  vehicle(id: Int!): Vehicle!
  vehicleByRegistration(registrationNumber: String!): Vehicle!
  vehicleHistory(vehicleId: Int!): [ParkingTicket!]!
  vehicles: [Vehicle!]!
}

input SignUpInput {
  email: String!
  fullName: String!
  password: String!
}

enum TicketStatus {
  ACTIVE
  CANCELLED
  COMPLETED
}

input UpdateParkingLotInput {
  address: String
  city: String
  code: String
  country: String
  id: Int!
  latitude: Float
  longitude: Float
  name: String
  postalCode: String
  state: String
}

input UpdateProfileInput {
  fullName: String
  phone: String
}

input UpdateVehicleInput {
  color: String
  manufacturer: String
  model: String
  ownerId: Float
  registrationNumber: String
  type: VehicleType
}

type User {
  createdAt: DateTime!
  email: String!
  fullName: String!
  id: ID!
  isEmailVerified: Boolean!
  lastLoginAt: DateTime
  phone: String
  role: UserRole!
  status: UserStatus!
  updatedAt: DateTime!
}

enum UserRole {
  ADMIN
  CUSTOMER
  OPERATOR
}

enum UserStatus {
  ACTIVE
  BLOCKED
  INACTIVE
}

type Vehicle {
  color: String!
  createdAt: DateTime!
  id: ID!
  manufacturer: String!
  model: String!
  registrationNumber: String!
  status: VehicleStatus!
  type: VehicleType!
  updatedAt: DateTime!
}

enum VehicleStatus {
  ACTIVE
  INACTIVE
}

enum VehicleType {
  BIKE
  BUS
  CAR
  ELECTRIC_CAR
  SUV
  TRUCK
}